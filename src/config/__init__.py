# Per-dataset configuration (region sizes, class/channel counts, signal length).
# The shared harness/models read these values as parameters; there is no ECG200
# config module (ECG200 values currently live inline in its own code).
